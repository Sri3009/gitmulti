pipeline {
    agent any
    stages {
stage('checkout') {
            steps {
                sh 'git branch child3'
                sh 'git checkout child3'
         
                
            }
        }
    }
}

