pipeline {
    agent any
    stages {
stage('checkout') {
            steps {
                sh 'git branch child2'
                sh 'git checkout child2'
         
                
            }
        }
    }
}

